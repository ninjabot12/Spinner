/**
 * Claw Machine Feature - Public API
 * Re-exports main component and types for use elsewhere in the app.
 */

export { ClawMachine } from './ClawMachine';

export type {
  Prize,
  PlayResult,
  ClaimResult,
  Rarity,
  RewardType,
  MachineState,
  AnimationTimings,
  Geometry,
} from './types';
